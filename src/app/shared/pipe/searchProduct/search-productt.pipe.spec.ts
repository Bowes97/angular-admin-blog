import { SearchProducttPipe } from './search-productt.pipe';

describe('SearchProducttPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchProducttPipe();
    expect(pipe).toBeTruthy();
  });
});
